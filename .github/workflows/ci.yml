name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Swift tests
        run: swift test
      - name: Xcode build
        run: xcodebuild -project Quickwheel.xcodeproj -scheme Quickwheel -configuration Debug build
